/**
 * Общие константы питомца — единый источник правды для backend (валидация)
 * и Mini App (ограничение поля ввода). Кастомизация имени/внешнего вида —
 * платная фича (PremiumFeature.PET_CUSTOMIZATION).
 */

/** Минимальная/максимальная длина имени питомца (после trim). */
export const PET_NAME_MIN_LENGTH = 1;
export const PET_NAME_MAX_LENGTH = 24;

/**
 * Жёсткий потолок числа питомцев на пользователя (мульти-петы, премиум).
 * Защищает масштаб (ограничивает число строк Pet/PetState на пользователя)
 * независимо от премиум-гейта; проверяется в сервисе. Бесплатный план — 1
 * (авто-созданный питомец), доступ к созданию >1 гейтится PremiumFeature.MULTI_PET.
 */
export const MAX_PETS_PER_USER = 10;
